<h1>Edit Wiki</h1>

<%= form_with(model: @wiki, local: true) do |form| %>

<% if @wiki.errors.any? %>
<div>
  <h2>
    <%= pluralize(@wiki.errors.count, "error") %> prohibited this artilce from being saved:
  </h2>
  <ul>
    <% @wiki.errors.full_messages.each do |msg| %>
    <li><%=  msg %></li>
    <% end %>
  </ul>
</div>
<% end %>

<p>
  <%= form.label :title %><br>
  <%= from.text_field :title %>
</p>
<p>
  <%= form.label :body %><br>
  <%= from.text_area :body %><br>
</p>
<p>
  <%= form.submit %>
</p>
<% end %>
<%= link_to 'Back', wikis_path %>
